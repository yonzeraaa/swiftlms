# SwiftLMS Backup — Environment Variables
# Copy to .env.backup (not committed) and fill in real values.
# These same vars must be set in Vercel for the UI-triggered backup to work.

# Supabase PostgreSQL connection string (used only by the cron pg_dump)
# Found in: Supabase dashboard → Settings → Database → Connection string (URI)
DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# Supabase project URL (already in .env.local, repeated here for standalone cron use)
NEXT_PUBLIC_SUPABASE_URL=https://[project-ref].supabase.co

# Service role key — bypasses RLS (keep secret, never expose to client)
# Found in: Supabase dashboard → Settings → API → service_role key
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Google Service Account credentials (JSON-stringified)
# Steps:
#   1. Google Cloud Console → IAM → Service Accounts → Create
#   2. Grant it "Editor" or a custom role with Drive access
#   3. Create a JSON key → download → paste the content as a single line below
#   4. Share the backup Google Drive folder with the service account email
GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"..."}

# ID of the Google Drive folder where backups will be created
# Get it from the folder URL: drive.google.com/drive/folders/<FOLDER_ID>
GOOGLE_DRIVE_BACKUP_FOLDER_ID=1AbCdEfGhIjKlMnOpQrStUvWxYz
